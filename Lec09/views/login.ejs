<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Authentication</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="h-screen flex flex-col text-white w-full bg-zinc-900">
      <form
        class="py-5 px-5 flex flex-col absolute top-0 right-0 left-0"
        action="/login"
        method="post"
      >
        <h2
          class="text-center text-4xl text-teal-200 underline uppercase italic"
        >
          Login your Account
        </h2>
        <input
          class="px-5 py-2 border-2 my-2 border-zinc-800 outline-none bg-transparent"
          type="email"
          placeholder="Email"
          name="email"
          required
        />

        <input
          class="px-5 py-2 border-2 my-3 border-zinc-800 outline-none bg-transparent"
          type="password"
          placeholder="Password"
          name="password"
          required
        />
        <button
          type="submit"
          class="px-8 py-2 border-2 border-zinc-800 rounded-lg outline-none bg-blue-500"
          value="Login"
        >
          Login
        </button>
      </form>
    </div>
  </body>
</html>
